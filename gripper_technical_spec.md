# Robotiq 2F-85 夹爪技术规格文档

## 一、通信接口

### 1.1 物理连接
- **接口类型**: USB 转串口（USB-to-Serial）
- **设备路径**: 
  - Linux: `/dev/ttyUSB0`, `/dev/USB1`
  - Windows: `COM1`, `COM2`, ...
  - macOS: `/dev/cu.usbserial-*`

### 1.2 串口参数
- **波特率**: 115200
- **数据位**: 8
- **停止位**: 1
- **校验位**: 无
- **超时**: 200ms

### 1.3 通信协议
- **协议**: Modbus RTU over Serial
- **设备地址（Unit ID）**: `0x0009` (十进制 9)

## 二、Modbus 寄存器地址

### 2.1 命令寄存器（写入）
- **起始地址**: `0x03E8` (十进制 1000)
- **寄存器数量**: 3 个（6 字节数据）
- **功能**: 发送控制命令到夹爪

### 2.2 状态寄存器（读取）
- **起始地址**: `0x07D0` (十进制 2000)
- **寄存器数量**: 3 个（6 字节数据）
- **功能**: 读取夹爪当前状态

## 三、命令格式（6 字节）

### 3.1 字节定义

| 字节 | 名称 | 范围 | 说明 |
|------|------|------|------|
| 0 | 控制标志 | 0-255 | rACT, rGTO, rATR 标志位 |
| 1 | 保留 | 0 | 固定为 0 |
| 2 | 保留 | 0 | 固定为 0 |
| 3 | rPR | 0-255 | 位置请求值 |
| 4 | rSP | 0-255 | 速度值 |
| 5 | rFR | 0-255 | 力值 |

### 3.2 控制标志位（字节 0）

| 位 | 名称 | 说明 |
|---|------|------|
| 0 | rACT | 激活标志：0=未激活，1=激活 |
| 3 | rGTO | 执行运动：0=停止，1=执行 |
| 4 | rATR | 紧急释放：0=正常，1=紧急释放 |

### 3.3 参数映射

#### 位置（rPR）
- **物理范围**: 0.0 m（全闭） ~ 0.085 m（全开）
- **寄存器值**: 230（全开） ~ 3（全闭）
- **转换公式**: `rPR = (3.0 - 230.0) / 0.085 * width + 230.0`

#### 速度（rSP）
- **物理范围**: 0.013 m/s ~ 0.1 m/s
- **寄存器值**: 0 ~ 255
- **转换公式**: `rSP = 255.0 / (0.1 - 0.013) * speed - 0.013`

#### 力（rFR）
- **物理范围**: 0% ~ 100% 最大力
- **寄存器值**: 0 ~ 255
- **转换公式**: `rFR = 255.0 / 100.0 * force_percent`

## 四、状态格式（6 字节）

### 4.1 字节定义

| 字节 | 名称 | 说明 |
|------|------|------|
| 0 | 状态标志 | gACT, gGTO, gSTA, gOBJ |
| 1 | 保留 | 未使用 |
| 2 | gFLT | 故障代码 |
| 3 | gPR | 位置请求值（回显） |
| 4 | gPO | 位置反馈值（实际位置） |
| 5 | gCU | 电流值 |

### 4.2 状态标志位（字节 0）

| 位 | 名称 | 值 | 说明 |
|---|------|-----|------|
| 0 | gACT | 0/1 | 激活状态：0=未激活，1=激活 |
| 3 | gGTO | 0/1 | 运动执行状态：0=停止，1=执行中 |
| 4-5 | gSTA | 0-3 | 夹爪状态：0=复位，1=激活中，2=保留，3=就绪 |
| 6-7 | gOBJ | 0-3 | 物体检测：0=运动中，1=停止无物体，2=停止有物体，3=保留 |

### 4.3 故障代码（gFLT，字节 2）

| 值 | 说明 |
|---|------|
| 0 | 无故障 |
| 其他 | 参考 Robotiq 故障代码表 |

### 4.4 位置反馈（gPO，字节 4）

- **寄存器值**: 230（全开） ~ 3（全闭）
- **转换公式**: `width = 0.085 / (3.0 - 230.0) * (gPO - 230.0)`

### 4.5 电流值（gCU，字节 5）

- **单位**: 0.1 安培/单位
- **实际电流**: `current = gCU * 0.1` 安培

## 五、初始化流程

### 5.1 连接步骤
1. 打开串口连接（115200 波特率）
2. 验证 Modbus 通信（读取状态寄存器）
3. 激活紧急释放（rATR=1, rARD=0）
4. 取消紧急释放（rATR=0）
5. 激活夹爪（rACT=1）
6. 验证激活状态（gSTA=3, gACT=1）

### 5.2 激活命令
- **rACT**: 1（激活）
- **rPR**: 0
- **rSP**: 255（最大速度）
- **rFR**: 150（中等力）

## 六、控制流程

### 6.1 发送命令
1. 构建 6 字节命令数组
2. 将字节打包成 3 个 16 位寄存器
3. 通过 Modbus 写入寄存器 `0x03E8`
4. 设置 Unit ID 为 `0x0009`

### 6.2 读取状态
1. 通过 Modbus 读取寄存器 `0x07D0`（3 个寄存器）
2. 设置 Unit ID 为 `0x0009`
3. 解包寄存器为 6 字节
4. 解析状态字节获取信息

## 七、状态判断

### 7.1 夹爪就绪
- **条件**: `gSTA == 3` 且 `gACT == 1`

### 7.2 是否运动
- **条件**: `gGTO == 1` 且 `gOBJ == 0`

### 7.3 是否检测到物体
- **条件**: `gOBJ == 1` 或 `gOBJ == 2`

### 7.4 是否停止
- **条件**: `gOBJ != 0`

## 八、典型参数值

### 8.1 默认速度
- **慢速**: rSP = 128（约 0.05 m/s）
- **中速**: rSP = 200（约 0.08 m/s）
- **快速**: rSP = 255（0.1 m/s）

### 8.2 默认力
- **轻柔**: rFR = 50（约 20% 最大力）
- **中等**: rFR = 150（约 60% 最大力）
- **强力**: rFR = 255（100% 最大力）

### 8.3 典型位置
- **全开**: rPR = 230（0.085 m）
- **半开**: rPR = 116（0.0425 m）
- **全闭**: rPR = 3（0.0 m）

## 九、错误处理

### 9.1 通信失败
- **现象**: Modbus 读取返回 `None`
- **处理**: 重试或使用上次有效状态

### 9.2 激活失败
- **现象**: `gSTA != 3` 或 `gACT != 1`
- **处理**: 重新执行激活流程

### 9.3 故障检测
- **检查**: `gFLT != 0`
- **处理**: 根据故障代码采取相应措施

## 十、关键参数总结

| 参数 | 值 | 说明 |
|------|-----|------|
| 串口波特率 | 115200 | 固定值 |
| Modbus Unit ID | 0x0009 | 固定值 |
| 命令寄存器 | 0x03E8 | 写入地址 |
| 状态寄存器 | 0x07D0 | 读取地址 |
| 最大开度 | 0.085 m | 85mm |
| 位置全开值 | 230 | 寄存器值 |
| 位置全闭值 | 3 | 寄存器值 |
| 速度范围 | 0.013-0.1 m/s | 物理范围 |
| 力范围 | 0-100% | 百分比 |

## 十一、依赖库

### 11.1 必需库
- **pymodbus**: Modbus RTU 客户端（版本 2.5.3 推荐）
- **pyserial**: 串口通信支持

### 11.2 可选库
- **numpy**: 数值计算（用于参数转换）

## 十二、注意事项

1. **串口权限**: Linux 系统需要用户加入 `dialout` 组或设置设备权限
2. **超时设置**: Modbus 读取超时建议设置为 200ms
3. **数据打包**: 写入时需将 6 字节打包成 3 个 16 位寄存器
4. **数据解包**: 读取时需将 3 个寄存器解包成 6 字节
5. **激活等待**: 激活命令后需等待 1-2 秒让夹爪响应
6. **状态验证**: 每次操作后应验证状态寄存器确认执行成功

